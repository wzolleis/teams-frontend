import * as React from 'react';

type AboutProps = {};

export class About extends React.Component<AboutProps> {
    render() {
        return (
            <h1>Dojo Example with React and Typescript</h1>
        );
    }
}